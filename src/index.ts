// Export moment-based utilities
export { formatDate as momentDate } from "./moment-date";

// Export dayjs-based utilities
export { formatDate as dayjsDate } from "./dayjs-date";

// You can also export a default based on preference
export { formatDate as formatDate } from "./moment-date";
